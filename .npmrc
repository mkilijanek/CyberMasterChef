engine-strict=true
strict-peer-dependencies=true
auto-install-peers=true
# Supply-chain: rozważ ustawienia pnpm z dokumentacji:
# - allowBuilds / allow list (dla binarek)
# - minimumReleaseAge
# - trustPolicy
#
# Ten scaffold nie wymusza ich twardo, bo część zależności (np. bundlery)
# bywa zależna od skryptów instalacyjnych. Zasady są w docs/ i SECURITY.md.
